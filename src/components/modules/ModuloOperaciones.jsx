import { Calculator } from 'lucide-react';
import PlaceholderModule from '../PlaceholderModule';

export default function ModuloOperaciones() {
  return (
    <PlaceholderModule
      icon={Calculator}
      title="Operaciones Binarias"
      description="Realiza sumas, restas, multiplicaciones y operaciones lógicas (AND, OR, XOR, NOT) directamente en sistema binario con representación visual de cada paso."
    />
  );
}
