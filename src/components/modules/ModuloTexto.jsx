import { Type } from 'lucide-react';
import PlaceholderModule from '../PlaceholderModule';

export default function ModuloTexto() {
  return (
    <PlaceholderModule
      icon={Type}
      title="Representación de Texto"
      description="Explora cómo los caracteres se representan internamente mediante tablas ASCII y Unicode. Convierte texto a su equivalente binario y hexadecimal."
    />
  );
}
