import { ArrowRightLeft } from 'lucide-react';
import PlaceholderModule from '../PlaceholderModule';

export default function ModuloConversiones() {
  return (
    <PlaceholderModule
      icon={ArrowRightLeft}
      title="Conversor de Bases"
      description="Convierte números entre los sistemas binario, octal, decimal y hexadecimal. Visualiza el proceso paso a paso para comprender cada conversión."
    />
  );
}
