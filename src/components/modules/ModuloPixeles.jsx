import { Palette } from 'lucide-react';
import PlaceholderModule from '../PlaceholderModule';

export default function ModuloPixeles() {
  return (
    <PlaceholderModule
      icon={Palette}
      title="Píxeles y Colores"
      description="Comprende cómo se representan los colores en formato RGB y hexadecimal. Experimenta con valores de píxeles y observa la conversión en tiempo real."
    />
  );
}
